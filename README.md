# Online Survey Platform (Lab Mini-Project)

A simple, responsive, and dynamic Full Stack style mini-project built directly within a browser logic model to demonstrate Core JavaScript concepts natively. This project strictly demonstrates the usage of multiple Input Types, Form Validations, JavaScript Arrays of Objects, DOM Manipulation, and Dynamic Table logic routines ideal for an academic structural lab submission.

## ✅ Requirements Fulfilled Completely

1. **Vanilla Core Framework Usage Only**: Native HTML5, pure CSS3 styling, and strictly Vanilla JS implementations - No external libraries like React or Vue. 
2. **HTML Form Control Layouts**: Fully featured dynamic input form covering multiple complex user components:
   * **Text, Email, Length Variables** (Includes min/max number attributes and html constraint limits natively via `required`).
   * **Radio Elements** (Experience and UX rating arrays logic routines).
   * **Checkbox Sets** (Multi-select node list models utilizing arrays internally).
   * **Selective Dropdown Elements**.
   * **Feedback Text Areas**.
3. **Array of Objects Models**: Efficiently captures and transforms user payloads directly into distinct normalized JavaScript JS Object maps stored continuously inside a primary array model representation.
4. **Iterative Loops Control Constraints (`for` / `forEach`)**: Heavily commented, distinct usage models for core javascript loops arrays mappings:
   * Used heavily to extract form NodeLists array bindings (`querySelectorAll().forEach`).
   * Used to map array sets physically into structured DOM Element Table rows maps directly (`for (let i = 0... )`).
   * Computation engines processing Array values dynamically through mathematics aggregates directly (`for...of`, `for...in`).
5. **Responsive Fronted Styling Methods**: Includes cleanly designed flexbox constraints, UI element cards logic rendering contexts, button interactions logic mapping, and structured mobile-responsive visual boundaries.
6. **Dynamic Dashboard and Tables UI Display**: Converts normalized payload entries specifically into responsive Table UI arrays. Extrapolates aggregated metrics dynamically internally into the Statistic metric Cards framework native layers logic handling structures.

## 📂 Internal Routing And File Configurations

* `index.html`: Houses navigation routing DOM IDs templates, nested user Form bindings, hidden table view bindings and card interfaces mapping structures.
* `style.css`: Contains CSS pseudo classes (`:hover`, `:focus`), responsive Grid/Flex variables mappings context definitions natively, keyframes mappings, and normalized UI boundaries sets.
* `script.js`: Native execution scripting maps managing default `e.preventDefault()` submit captures, DOM extraction methodologies (`document.getElementById`), Node array configurations logic matrices loops loops implementations, array mapping object constructions representations layouts formats, and dynamic computation UI overrides.
* `README.md`: Included lab submission and usage descriptive manual mapping boundaries structurally.

## ⚙️ How to Review / Run Successfully

1. Simply double-click on `index.html` locally using any standard modern Internet browser environment explicitly (Chrome, Firefox, Safari natively). No local server is mandatory strictly due to the fact everything renders seamlessly mapped.
2. Fill out at least two test variables inside the core visual survey module completely securely (utilize the input variations specifically arrays matrices loops combinations natively representations). Hit the submit function handler representation models.
3. Once completed natively, locate and physically execute clicking logic against the **"Results Dashboard"** navigation link on the top right UI menu matrices framework representations explicitly.
4. View the JavaScript computed computations configurations internally resolving loop logics seamlessly across populated dynamic statistical cards natively mapped UI and detailed physical iteration outputs bound precisely natively against dynamic HTML Tables physically bindings logically interfaces interfaces explicitly mapping matrix logics constraints natively constraints logic matrices representations representations mapping.
